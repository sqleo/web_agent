# 可复制为 `.env` 或 `.env.local`（本地 `next dev`）；仅用 Docker / 服务器部署时再复制一份为 `.env_web`，与后端目录里的 `.env` 区分开。
#
# 接口根地址（不要末尾斜杠；勿写 /v1，代码会自动加）。实际请求：{NEXT_PUBLIC_API_BASE_URL}/v1/auth/login 等
# 流式对话：POST {NEXT_PUBLIC_API_BASE_URL}/v1/agent/chat/stream
# 删除对话：DELETE {NEXT_PUBLIC_API_BASE_URL}/agent/chat/{thread_id}
# 暂停/继续：POST .../agent/chat/{thread_id}/pause | /resume
# 历史与时间旅行：GET .../history ； POST .../travel
# 监控：GET {BASE}/monitor/overview、trends/*、errors、models、requests
# Agent 工具：GET {BASE}/agent/tools ； PUT {BASE}/agent/tools/settings
NEXT_PUBLIC_API_BASE_URL=http://localhost:8888
# 服务器上前端若与后端同机（示例 IP），可按实际端口改，并用于构建/运行：
# NEXT_PUBLIC_API_BASE_URL=http://123.207.188.25:8888

# —— Docker / 腾讯云 TCR（生产）——
# 构建并推送：npm run docker:build-push（默认 linux/amd64、命名空间 sqliu；会先加载 .env_web 里的变量）
# 构建前设置后端公网地址（会打进前端 bundle，改地址需重新 build）：
#   NEXT_PUBLIC_API_BASE_URL=https://your-api.example.com npm run docker:build-push
# 可选：TAG=v1 IMAGE_NAME=web_agent NAMESPACE=sqliu
# 服务器 compose 里镜像全名，默认与构建脚本一致：
# TCR_IMAGE=ccr.ccs.tencentyun.com/sqliu/web_agent:latest
# 宿主机映射端口（默认 3000）：
# HOST_PORT=3000
